{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "label": "Build",
        "command": "cmake -S . -B ${workspaceFolder}/build && cd ${workspaceFolder}/build && make",
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "detail": "Build"
      },
      {
        "label": "Run",
        "type": "shell",
        "command": "cmake -S . -B ${workspaceFolder}/build && cd ${workspaceFolder}/build && make && ./MinecraftCppGL",
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "group": {
          "kind": "build",
          "isDefault": true
        },
      },
    ]
  }